# 修复方案（v3）：8 项已核实问题的落地改法

> **基线声明。** 本文以主线工作树 `D:\学习笔记\Agent\agent-tool` 为准（HEAD `f257c91`）。
> 工作树仅本文档 + `docs/17-审查台账.md` + 2 个与本仓无关的产物（`rusty-helmet-bioluminescent.miora` 及其 `_assets/`）未跟踪。
>
> **关键文件指纹（md5 前 8 位）** —— 行号对不上时，先核指纹，再核代码，最后才怀疑结论：
>
> | 文件 | 指纹 | 文件 | 指纹 |
> | --- | --- | --- | --- |
> | `src/client/client.py` | `e3320e93` | `src/schema/schema.py` | `e3679af7` |
> | `src/voice/tts.py` | `142d14d9` | `src/streamlit_app.py` | `103dc10b` |
> | `src/agents/utils.py` | `a26fae35` | `src/agents/interrupt_agent.py` | `30eead79` |
> | `src/run_agent.py` | `e290a254` | — | — |
>
> **版本沿革。** v1 误把另一棵 worktree 当基线（第 7 项被错判为「不存在」）→ v2 修正基线、
> 给出 8 项改法 → v3：8 项全部**独立复核**通过（不是采信上游结论），补齐 v2 漏掉的测试与
> 文档影响面，行号重新校准 → **v4（本文）**：对 v3 再做一轮逐行复核（7 个指纹复测一致、
> 全量行号实测、函数计数实测、`var/verify_dup_functions.py` 复跑），修正 16 处偏差，清单见 §0.5。
>
> **性质。** 8 项里只有第 7 项是正确性缺陷；其余是重复、死代码、分层与文档债。
> 除第 7 项外**都不该混进同一个提交**。
>
> **与 `docs/17` 的关系。** 盘上已有一份 `docs/17-审查台账.md`（同一 HEAD `f257c91`、同一指纹基准，独立成稿）。
> 本文是**落地改法**（怎么改、改哪里、怎么验收），`docs/17` 是**证据台账**（复核手段、每条结论的取证过程）。
> 两者结论一致，属同一轮审核的两份产出，互不取代。

---

## 0. 复核结论

### 0.1 八项独立复核

复核手段：`wc -l` 行数、md5 比对、`ast` 抽源码做精确/去注释两档分组
（脚本 `var/verify_dup_functions.py`）、pydantic 实测、读安装版 langgraph / streamlit 源码。

| # | 项 | 结论 | 我实际拿到的证据 |
| --- | --- | --- | --- |
| 1 | client 三对同步/异步重复 | ✅ 真 | `ainvoke:88`/`invoke:134`、`stream:204`/`astream:261`、`get_user_threads:376`/`aget_user_threads:401`；`_parse_stream_line:179` 与 `_user_threads_request:369` 已抽过，剩下的重复就是 HTTP 调用 + 错误包装 |
| 2 | 7 个框架函数重复 | ✅ 真（措辞需修正，见 §0.3） | 7 个函数名；`wrap_model` **7 处**（`interrupt_agent` 那版带 `system_prompt`、独立形态，不算重复集）、`acall_model` **6 处**、`pending_tool_calls` **4 处** |
| 3 | `stt=157` / `tts=173` | ✅ 真 | `wc -l` 实测 157 / 173 |
| 4 | `run_agent.py` 注释块 29-36、`cast:16`、`CompiledStateGraph:9` | ✅ 真 | 8 行 pygraphviz 注释；`cast` 在第 16 行确实在用；`from langgraph.graph.state import CompiledStateGraph` 在第 9 行 |
| 5 | `CustomData` 无 `type`，`task.py` 传 `type=` 被静默丢弃 | ✅ 真 | `agents/utils.py:8-11` 只有 `data` 一个字段；`task.py:25` 传 `type=self.name`。实测 pydantic **2.13.4**：构造不报错，`model_dump()` 只剩 `{'data': ...}`，`model_config == {}`（extra 默认 ignore） |
| 6 | `pretty_repr:126`/`pretty_print:136`、`TaskDataStatus:37` | ✅ 真 | `schema/schema.py:126,136`；`schema/task_data.py:37-74`（38 行） |
| 7 | 蒸馏两条路径都实现、读取侧都断 | ✅ 真 | 见 §0.4 第 1 条：`apply_distillation` 全仓调用点**只有 `service.py:256` 一处**，`agents/` 零调用；`distilled_summary` 读取点只有 `service.py:252` 与写节点自己 |
| 8 | `isinstance(result, list)` 死分支 | ✅ 真 | `interrupt_agent.py:103`；`BaseStore.get`/`aget` 返回注解均为 `Item \| None`（`store/base/__init__.py:762,1007`，batch mixin `base/batch.py:88` 同），`src/memory/*` 无任何 `aget` 覆写 |

### 0.2 三条硬证据（本轮新拿，v2 没有）

1. **streamlit 确在同线程执行缓存函数** —— 决定第 1 项的改法是必修还是可选。
   `streamlit 1.59.2`，`runtime/caching/cache_utils.py:385`：
   `computed_value = self._info.func(*func_args, **func_kwargs)` —— 直接在当前线程调用，
   **没有线程池卸载**。`streamlit_app.py:178` 的 `fetch_user_threads_cached(...)` 位于
   `async def main()`（`:81`）内部，而脚本由 `asyncio.run(main())`（`:718`）驱动。
   → 同步方法里裸用 `asyncio.run` 必抛 `cannot be called from a running event loop`。
   **线程兜底是必需的，不是防御性冗余。**
2. **`apply_distillation` 的调用点唯一性**（第 7 项的判决性证据）。
   `grep -rn apply_distillation src/`（排除 `.pyc`）共 **7 处**：定义 `core/distill.py:320`、
   docstring 提及 4 处（`distill.py:29,268,313`、`service.py:242`）、导入 `service.py:34`、
   实际调用 `service.py:256`。**`agents/` 目录零调用** —— 全仓唯一的调用点就是 `service.py:256`。
3. **`add_messages` 的净效果是增量而非折叠**（第 7 项严重性）。`apply_distillation` 返回
   `[build_summary_message(summary), *tail]`，其中 `tail` 是**原消息副本（同 id）**，
   而摘要由 `build_summary_message` **新建、无 id**。过 `add_messages` 后：同 id 原地替换、
   无 id 分配新 id 并**追加到末尾**。所以 `service.py:217-222` 那句「改写的是送给模型的这一份」
   与实际语义相反 —— 历史折不掉，且**每轮多囤一条摘要副本**（`24→27→30→33→36`，
   与 `docs/15` §7.2.3 实测一致）。

### 0.3 对上游审计措辞的三处修正

| # | 上游措辞 | 修正 |
| --- | --- | --- |
| 2 | 「7 框架函数**逐字**重复」 | 函数名对（7 个），但逐字程度分三档：**5 个含注释逐字**（`format_safety_message` / `safeguard_input` / `block_unsafe_content` / `check_safety` / `pending_tool_calls`）；`acall_model` 只差一句中文注释；**`wrap_model` 已经分化、不算重复**（`code_reviewer` 那版多注入 `recalled_reviews`）。另：覆盖面比结论**更大**（`wrap_model` 7 / `acall_model` 6 / `pending_tool_calls` 4 处，不是 3 / 3 / 3） |
| 6 | 「存在」 | 存在属实，但这是**归位**不是删减，净 LOC ≈ 0，**不应记进重构收益**；且有 v2 漏掉的 README 连带项（见 §9.2） |
| 7 | 「断点集中于 acall_model 未调 + 折叠进 state 冲突」 | 两半都对，但严重性被低估：service 路径不是「无效」，是**净增量**（每轮 +1 条摘要副本，checkpoint 无界增长） |

### 0.4 三处行号漂移（v2 → v3 校准）

| 位置 | v2 写的 | 实际 | 影响 |
| --- | --- | --- | --- |
| `pyproject.toml` `bad-specialization` | `:126` | **`:129`** | 无（注释在 `:126-128`，措辞只说「每个 agent」，未给计数，**不必改**） |
| `docs/15` §7.2.3 引用的 `code_reviewer.py:59` | — | 行号 `:59` **本来就准**——那是 `wrap_model` 内 `build_messages` 拼 `[system] + state["messages"]` 的 return 行；错的只是函数名标注（写成 `acall_model`，实为 `wrap_model`） | 无（`docs/15` 是历史记录，可在第 7 项落地时顺手把函数名改对；**不要改成 `:73`**——`acall_model` 只是调用 `wrap_model`，拼接不发生在它自己身上） |
| streamlit 版本 | `1.56` | **`1.59.2`** | 无（结论方向不变，同线程执行已复核） |

### 0.5 v4 复核修正清单（v3 → 本文）

对 v3 逐行实测后的 16 处偏差。行号类以当前工作树为准，指纹见文首表。

| # | 位置 | v3 写的 | 实测 | 处置 |
| --- | --- | --- | --- | --- |
| 1 | §0.2 第 2 条 | `apply_distillation` 「只命中三处」 | 实为 **7 处**：定义 `distill.py:320`、docstring 4 处、导入 `service.py:34`、调用 `service.py:256` | 已改，并补回被漏掉的导入行 |
| 2 | §2 | `type: ignore` 在 `service.py:243`、`:278` | 实际在 **`:289`**、**`:324`**；`:243`/`:278` 是 docstring 正文 | 已改 |
| 3 | §2 | `docs/10:169` 引用 `run_agent.py` | 该引用在 **`:174`**（`:169` 是空行） | 已改 |
| 4 | §2 | 引文 `From langgraph.graph.state import` | 源码第 9 行是小写 `from` | 已改 |
| 5 | §1 | `tests/service/conftest.py:52` | `patch("service.service.get_agent", ...)` 在 **`:51`**，`:52` 是 `yield` | 已改 |
| 6 | §4.2 | 「`:155` 与 `:163` 的两个 `interrupt(...)`」 | 实际在 **`:156`**（`:155` 是 `if` 条件）与 **`:167-169`**（`:163` 是 `try:`） | 已改 |
| 7 | §5 | 净收益 ≈ 70 行 | 口径自相矛盾：60+25+25=110，减不出 330−70=260。实测两文件逐字相同代码行 **29**、相似度 **60.4%**，docstring 占 79/86 行 → 净变化约**持平** | 已改为「骨架收敛、LOC 持平」 |
| 8 | §6.2 第 1 步 | 只写「视图构造里加 `apply_distillation`」 | `code_reviewer.py` **没有**导入它（只在 State 里声明 `distilled_summary: str`，`:28`） | 已补「加导入」步骤 |
| 9 | §6.2 第 3 步 | 删 4 条用例 | **漏第 5 条** `test_handle_input_does_not_distill_on_interrupt_resume`（`:340`） | 已补，附处置建议 |
| 10 | §6.4 | `history = _history(30)`，注释引 `KEEP_MESSAGES` | 该测试文件**没有** `_history`（唯一辅助是 `:24 _index`）；常量实名是 `settings.DISTILL_KEEP_MESSAGES` | 已改为内联构造 |
| 11 | §6.6 | 「把 `code_reviewer.py:59` 校准为 `:73`」 | 与 §0.4「**不要改成 `:73`**」**直接冲突**；§0.4 是正确的 | 已删该句，改为只修函数名 |
| 12 | §6.6 | `docs/09:148` 「更新计数」 | 现为 26，删 4 条后应为 **22** | 已写明目标值 |
| 13 | §7.1 | research / rag「7 个函数全逐字」 | 脚本实测 `acall_model` 的逐字（含注释）相同组为**无** → 应为 6 逐字 + 1 去注释后相同 | 已改，与 §0.3 对齐 |
| 14 | §7.2 / §7.4 | `done_node` 默认 `END`，§7.4 却给 loop_agent 传 `None` | 自相矛盾：按字面实现会得到指向 `None` 的条件边；loop_agent 的 done 边本就指向 `END` | 已改为默认 `END`，删掉 `None` 那个传参 |
| 15 | §7.3 | 用 `partial(_wrap_model, ...)` | §7.2 **从未定义** `_wrap_model` | 已在 §7.2 补定义 |
| 16 | §7.2 | `extra_nodes` 用可变默认值 `{}` | ruff 未启 bugbear（`extend-select = ["I", "U"]`），不会被拦；同仓 `task.py:32` 已有同类写法 | **不改**，仅标注 |

**复核未发现问题的部分**（同样是实证，不是默认通过）：文首 7 个指纹全部复测一致；§0.1 八项的行号（`client.py` 88/134/179/204/261/369/376/401、`run_agent.py` 9/16/29-36、`schema.py` 126/136、`task_data.py` 37-74、`interrupt_agent.py` 103）；函数计数 `wrap_model` 7 / `acall_model` 6 / `pending_tool_calls` 4；§6 全部行号（`service.py` 34/217-224/234-267/252/256、4 条用例 290/304/323/331、`:307`）；§7.1 六项行数 145/139/240/79/174/62；§8.3 全部打桩行与测试归属（57/64/79、147/169、374/389/400/410、282/292/300、322/334）；§8.2 的 `astream:315-317` 守卫确实存在、同步 `stream` 确实没有；§9 的 `run_client.py` 26/34/50/58、README 190-201/195/196-199；§11.4 的 README `:59`、`:108`。

---

## 1. 动手前必读：不能动的接缝

这一节是整份方案的安全边界。**任何一项改动若碰到下表任一行，必须先读依赖它的测试。**

| 接缝 | 位置 | 谁依赖 | 约束 |
| --- | --- | --- | --- |
| `service.service.get_agent` | `tests/service/conftest.py:51` | 113 处 + 另 12 处，分布在 9 个文件 | 新增共享工具**不要**放进会读 `get_agent` 的路径；`_distill_input` 当初带 `_` 前缀且留在 `service.py` 就是为了不破坏它（本次是**删除**，见 §6） |
| `agents.code_reviewer.wrap_model` | `tests/agents/test_memory_read_path.py:203,213` | 2 处直接 import 并调用 | 工厂化后 `code_reviewer` **必须继续导出** `wrap_model`（薄转发即可） |
| 图的节点名与边 | `tests/agents/test_code_reviewer.py:25`、`test_memory_read_path.py:249-252` | 结构守卫 | 节点名不变；`recall_reviews` 到 `model` 的**可达性**不变；`("distill_history","model")` 仍直接相连 |
| `voice.manager.SpeechToText.from_env` / `TextToSpeech.from_env` | `tests/voice/test_manager.py:31,32` | manager 测试直接 patch | `voice.stt` / `voice.tts` 的**模块路径与类名都不能变**（`voice/__init__.py:34-35`、`voice/manager.py:12-13` 也在 import） |
| 错误串 | `tests/voice/test_stt.py:22,29`、`tests/voice/test_tts.py:22,29` | 四处 `match=` 断言 | `Unknown STT provider: ...` / `Deepgram STT provider not yet implemented` / `Unknown TTS provider: ...` / `ElevenLabs TTS provider not yet implemented` 逐字保留 |
| `custom_data` 的线上形状 | `tests/service/test_service_e2e.py:49,68`、`test_service_real_graphs.py:236`；消费方 `streamlit_app.py:566` | 前后端契约 | 目前 `custom_data` == `TaskData.model_dump()`，**不得改变** |
| `schema.task_data.TaskData` | `bg_task_agent/task.py:21`、`streamlit_app.py:566` | 序列化契约 | 可搬的只有 `TaskDataStatus`（UI 类），`TaskData` 留在 schema |
| `httpx.*` 打桩缝 | `tests/client/test_client.py` | 5 个同步测试 | 并轨后其中 **3 个必须重写**，见 §8.3 |

每批收尾统一跑：

```bash
uv run pytest -q
uv run ruff check . && uv run ruff format --check .
uv run pyrefly check
```

---

## 2. 第 4 项：`run_agent.py` 删掉 8 行注释

**现状。** `src/run_agent.py:29-36` 是 8 行注释掉的 pygraphviz 指引（`brew install graphviz`…）。

**改法。** 整块删除。

**保留 `cast` 与 `CompiledStateGraph`。** `get_agent()` 返回
`AgentGraph = CompiledStateGraph | Pregel`（`agents/agents.py:26`），`cast` 是**收窄**；
同一 union 的调用在 `service.py:289`（`agent.ainvoke(...)  # type: ignore`）与 `:324`
（`agent.astream(  # type: ignore[no-matching-overload]`）都靠 `type: ignore` 兜着，
删了会直接报错。`:9` 的 import 同理。

**不要删整个文件。** `docs/10:174` 与 `code_reviewer.py:160` 的注释都把它当作「脱离服务直接调用图」的场景在引用。

**净收益** 8 行。**验收** `uv run pytest tests/agents -q && uv run pyrefly check`。

---

## 3. 第 5 项：`CustomData.type` 删 3 处实参

**结论：删实参，不补字段。** 依据 —— 消费方 `streamlit_app.py:566` 做的是
`TaskData.model_validate(msg.custom_data)`，靠 `TaskData` 自己的字段辨认，**没有任何地方读 `type`**；
任务名已由 `TaskData.name`（`task.py:21` 传 `name=self.name`）承载，`type=self.name` 是纯冗余意图。
补字段反而要改线上形状（见 §1 契约行）。

| 位置 | 现状 | 改后 |
| --- | --- | --- |
| `src/agents/bg_task_agent/task.py:25` | `type=self.name,` | 删该行 |
| `tests/service/test_service_e2e.py:15` | `CustomData(type="start", data={...})` | 删 `type=` 实参 |
| `tests/service/test_service_e2e.py:30` | `CustomData(type="end", data={...})` | 删 `type=` 实参 |

**不要顺手加 `extra="forbid"`。** `custom_data` 本来就是透传通道，收窄是正确性改动，
不属于本次「清理」范围 —— 加了会让上面三处从「静默忽略」变成「直接报错」，行为面完全不同。

**验收** `uv run pytest tests/service/test_service_e2e.py tests/agents -q`。

---

## 4. 第 8 项：`interrupt_agent` 删死分支 + 递归改循环

### 4.1 删死分支（`:98-107`）

现状是三层猜测嵌套：`if result:` → `if isinstance(result, list):` → `if result:`。
按 §0.1 的注解证据，list 分支**永不进入**。改为：

```python
        try:
            user_data = await store.aget(namespace, key=key)
```

`:100` 那句「处理 store.aget 可能直接返回 Item 或返回列表的情况」一并删掉 —— 它在替一个
不存在的重载写注释，留着会让下一个人继续以为存在 list 返回。

### 4.2 递归改循环（`:160`、`:173`）

两处 `return await determine_birthdate(state, config, store)` 是自递归，深度由用户输入次数决定
（无上界）。改为循环：

```python
    while True:
        response: BirthdateExtraction = await model_runnable.ainvoke(state, config)
        if response.birthdate is None:
            prompt = f"{response.reasoning}\nPlease tell me your birthdate?"
        else:
            try:
                birthdate = datetime.fromisoformat(response.birthdate)
                break
            except ValueError:
                prompt = (
                    "I couldn't understand the date format. "
                    "Please provide your birthdate in YYYY-MM-DD format."
                )
        state["messages"].append(HumanMessage(interrupt(prompt)))
```

配套调整：`:134-152` 的 `m` / `structured` / `model_runnable` 构造移到循环外（现在就在外面，保持）；
`:156` 与 `:167-169` 的两个 `interrupt(...)` **原地改写成赋值 `prompt`**（注意 `:155` 是
`if response.birthdate is None:`、`:163` 是 `try:`，都不是 interrupt 本身），
`state["messages"].append(...)` 上移到循环尾；`:185-190` 的 return 块接在 `break` 之后。

**刻意保留 `state["messages"].append(...)` 的就地语义。** `docs/10` 已覆盖两轮中断用例，
改成通过 `return` 更新属**正确性**范畴（会改变 graph state 的更新路径），另开一项，
不要混进这次清理。

**验收** `uv run pytest tests/agents/test_interrupt_agent.py tests/service/test_service_real_graphs.py -q`。

---

## 5. 第 3 项：STT / TTS 泛型基类

**现状。** `stt.py`(157) / `tts.py`(173) 共享同一骨架：
`__init__` → `_get_api_key` → `_load_provider`（match 分发 + 未实现分支 + 未知兜底）→
`provider` property → `from_env`（未设即关闭、异常吞掉返回 `None`）。
**真正的重复代码约 25 行/文件**，其余是 docstring（中文化后 docstring 占比很高）。

**改法。** 新增 `src/voice/factory.py`：

```python
class ProviderFactory(Generic[P]):
    """语音 provider 工厂的共享骨架。"""

    provider_env_var: ClassVar[str]
    api_key_env_vars: ClassVar[Mapping[str, str]]
    label: ClassVar[str]          # "STT" / "TTS"，用于拼错误串
    available: ClassVar[str]      # "openai"，用于拼 Unknown ... 的后半句

    def _get_api_key(self, provider, api_key): ...
    def _load_provider(self, provider, api_key, config): ...
    @property
    def provider(self) -> str: ...
    @classmethod
    def from_env(cls): ...
    def _build(self, provider, api_key, config): raise NotImplementedError
```

- `stt.py` / `tts.py` 保留**同名类**，改为继承 `ProviderFactory`，只实现 `_build` 与自己的委派方法
  （STT 的 `transcribe`、TTS 的 `generate` + `get_format`）。
- `label` 拼接保证错误串逐字不变：`f"Unknown {label} provider: {provider}. Available providers: {available}"`。
  TTS 的 `_build` 里保留自己的 config 抽取（`voice`/`model` 默认值）。
- 子类里必须各留一个「接下来怎么扩展」的注释位置，因为现有注释里写着 ElevenLabs / Deepgram
  的接入示例（`stt.py:96-98`、`tts.py:102-106`），这是**有意保留的引导**，不要顺手删。

**收益口径（v4 实测修正）。** 原写「净收益 ≈ 70 行」不成立 —— 其自述组件（基类 60 + 两个
子类各 25 = 110）推出的新体量就是 110 行，与 330−70=260 对不上。实测：两文件经 `ast` 去掉
docstring 与注释后各 **46 / 50** 行代码，其中逐字相同的代码行 **29** 行、相似度 **60.4%**；
docstring 另占 **79 / 86** 行（中文化后它才是主体）。按「每文件削掉 29 行重复、新增约 60 行
基类」估算，**净 LOC 约持平**。→ 本项收益应记作**「同一骨架收敛到单一维护点」**，别记行数；
对外报数时写明口径（是否含 docstring）并附实测值，不要沿用估算。

**为什么 `label` 而不是硬编码字符串：** §1 的四处 `match=` 断言要求
`Unknown STT provider: invalid` 与 `Unknown TTS provider: invalid` 都逐字成立。
注意 v2 只提了 `test_stt.py`，**`test_tts.py:22,29` 同样有**，两处都要覆盖。

**验收** `uv run pytest tests/voice -q`。

---

## 6. 第 7 项：蒸馏折叠收敛到唯一读取点（唯一正确性缺陷）

### 6.1 判决

`add_messages` 在语义上**无法**让 input 折叠掉既有 state（§0.2 第 3 条已用 id 语义证明）。
所以「改写 input」这条路不可能生效，**唯一可行的读取点是构造送给模型的那份视图**。

方法与 `docs/15` §7.2.3 的架构结论一致：**收敛到 model 节点 + 去掉 `_distill_input`**。

### 6.2 执行清单

| # | 动作 | 位置 |
| --- | --- | --- |
| 1 | 视图构造里加 `apply_distillation(state["messages"], state.get("distilled_summary") or "")` | 若第 2 项已做 → 工厂的 `build_messages`（§7.2）；未做 → `code_reviewer.wrap_model` 的 `build_messages`（`:50-59`） |
| 1b | 顶部补 `from core.distill import apply_distillation`（**现在没有这行** —— `code_reviewer.py` 只在 State 里声明了 `distilled_summary: str`，见 `:28`）。第 2 项落地后，此 import 随 `build_messages` 一起移到工厂模块。无环已验证：`core/llm.py`、`core/settings.py` 都只依赖 `core.settings` / `schema.models`，不 import `agents` | `code_reviewer.py` → 之后是工厂模块 |
| 2 | 删 `_distill_input` 定义（`:234-267`）与其调用（`:217-224`），并删 `service.py:34` 的 `from core.distill import apply_distillation` | `service.py` |
| 3 | 删它的 4 条专用用例：`test_distill_input_prepends_summary_view:290`、`_is_noop_without_summary:304`、`_is_noop_when_disabled:323`、`_is_noop_for_empty_thread:331` | `tests/core/test_distill.py` |
| 3b | **第 5 条不能照删**：`test_handle_input_does_not_distill_on_interrupt_resume`（`:340`）直接 import `service.service._handle_input`，读取侧一删，它的判据（「`input` 不是 dict」）就变成**恒真** —— 正是本仓明令禁止的「判据要能真的失败」。改为断言 `isinstance(input, Command)` 且 `resume` 等于用户输入，即**保留**这条对 `_handle_input` interrupt 分支的唯一覆盖 | `tests/core/test_distill.py:340-361` |
| 4 | 写侧**不动** | `code_reviewer.py:188-192`（节点注册）、`:214`、`:217` |
| 5 | 补新用例：断言**模型看到的条数已折叠**（spy model 捕获入参），而不是断言 `apply_distillation` 的返回值 | `tests/agents/test_memory_read_path.py` |

**必须用 `state.get("distilled_summary")` 而不是 `state["distilled_summary"]`。**
`test_memory_read_path.py:214` 直接传了一个不含该键的 dict 给 runnable；用下标会 KeyError。

### 6.3 顺序不用改

`distill_history`（`:192`）排在 `model` 之前：节点返回 `{"distilled_summary": summary}`
在同一轮先落进 state，同轮 `model` 就能看到新摘要。**不要把节点挪到 model 之后。**

### 6.4 新用例应当这样写（判据必须能真的失败）

```python
def test_model_sees_folded_view() -> None:
    """折叠必须发生在 model 的入参上 —— 这是 P0-7 唯一有意义的判据。

    反例：把 apply_distillation 从 build_messages 里去掉，本用例必须变红。
    """
    from core.llm import FakeToolModel

    captured: dict = {}

    class SpyModel(FakeToolModel):
        def invoke(self, input, config=None, **kwargs):  # type: ignore[override]
            captured["count"] = len(input) if isinstance(input, list) else None
            return super().invoke(input, config, **kwargs)

    from agents.code_reviewer import wrap_model

    # 本文件没有造历史的辅助函数（唯一辅助是 :24 的 `_index`），内联构造。
    # 30 条远超 `settings.DISTILL_KEEP_MESSAGES`（默认 12），必然触发折叠。
    history = [HumanMessage(content=f"第 {i} 条") for i in range(30)]
    runnable = wrap_model(SpyModel(responses=["ok"]))
    runnable.invoke({"messages": history, "distilled_summary": "摘要"})

    assert captured["count"] < len(history), "model 没有看到折叠后的视图"
```

**为什么不需要 `_distill_on` 那样的 fixture：** `apply_distillation`（`core/distill.py:338-343`）
**不受 `DISTILL_ENABLED` 门控** —— 它只看 `summary` 是否非空、以及 `keep = settings.DISTILL_KEEP_MESSAGES`
（默认 12）。门控在 `plan_distillation`（`:141`）与写节点里。所以本用例直接调 `wrap_model` 即可，
不依赖任何 monkeypatch。判据的可失败性也核对过：折叠后 count = 1 + 13 = 14 < 30（绿），
去掉折叠则 1 + 30 = 31（红）。

注意 `tests/core/test_distill.py:307` 已经记过同类教训：桩必须让被测分支**真的走到**，
否则用例是摆设。

### 6.5 验收

```bash
uv run pytest tests/core/test_distill.py tests/agents/test_memory_read_path.py tests/agents/test_code_reviewer.py -q
uv run python var/probe_p07_integration.py   # 退出码 0；1 = 缺陷仍在
uv run python var/probe_p07_invariants.py    # 5 条不变量仍绿（含反例注入）
```

> `var/p07_inject.py` 是注入器，**必须**带旁路备份 + 启动自愈。
> 实测过一次未完成的运行把注入留在 `src/core/distill.py`（切点前移循环被删），
> 之后所有探针只报「前置失败」而不提示源码已坏。

### 6.6 必须同步的文档

| 文件:行 | 现状 | 改后 |
| --- | --- | --- |
| `docs/01-架构说明.md:123` | 描述 `_distill_input` 的 input 折叠 | 改写为「model 节点构造视图时折叠」 |
| `docs/01-架构说明.md:181` | 记录「两条路径都没能让模型看到折叠视图」 | 删除或标为已修 |
| `docs/15` §7.2.3（`:718` 起） | 架构结论，标注**尚未执行** | 标为已执行，附修复后探针结果。另：`:722` 把 `code_reviewer.py:59` 标成 `acall_model`，**函数名应改为 `wrap_model`、行号保持 `:59` 不动**（`:59` 是 `wrap_model` 内 `build_messages` 的 return 行；`acall_model` 在 `:73`，它只是调用 `wrap_model`）—— 理由见 §0.4 |
| `docs/09-代码库审查Agent设计.md:148` | 用例计数含 `_distill_input`，现为 **26** | 删 4 条后应为 **22**（`:340` 那条按 §6.2 第 3b 步保留并改判据，不计入删除） |

改完按文档规范收尾：`python var/research/check_docs.py .` +
`python var/research/check_md_tables.py <file>` + `pymarkdown scan README.md docs/ -r`。

---

## 7. 第 2 项：图脚手架工厂

### 7.1 现状与分组

| 文件 | 行 | 情况 |
| --- | --- | --- |
| `research_assistant.py` | 145 | 6 个含注释逐字 + `acall_model` 去注释后相同（脚本判「逐字组 = 无」） |
| `rag_assistant.py` | 139 | 同上 |
| `code_reviewer.py` | 240 | 5 个逐字 + `wrap_model` 与 `acall_model` 已分化 |
| `loop_agent.py` | 79 | `wrap_model` / `pending_tool_calls` 逐字；`acall_model` 只差步数阈值与文案 |
| `knowledge_base_agent.py` | 174 | `wrap_model` / `acall_model` 形态相近但语义不同（文档注入） |
| `bg_task_agent.py` | 62 | `wrap_model` 是直通、`acall_model` 无步数保护 |

### 7.2 工厂签名与唯一视图构造点

```python
def _wrap_model(
    model: BaseChatModel,
    *,
    tools: Sequence[BaseTool],
    instructions: str,
    system_suffix: Callable[[AgentState], str] | None = None,
) -> RunnableSerializable[AgentState, AIMessage]:
    """构造「视图 → 绑定工具的模型」这条链。

    视图构造（含 §6.2 的蒸馏折叠）全仓**只有这一个实现**；各 agent 想改视图
    只能通过 `system_suffix` 钩子，改不了折叠本身。
    """

    def build_messages(state: AgentState) -> list:
        system = instructions
        if system_suffix:
            suffix = system_suffix(state)
            if suffix:
                system += suffix
        return [SystemMessage(content=system)] + apply_distillation(
            state["messages"], state.get("distilled_summary") or ""
        )

    preprocessor = RunnableLambda(build_messages, name="StateModifier")
    return preprocessor | model.bind_tools(tools)  # type: ignore[return-value]


def build_tool_agent_graph(
    *,
    tools: Sequence[BaseTool],
    instructions: str,
    system_suffix: Callable[[AgentState], str] | None = None,
    extra_nodes: Mapping[str, Callable[..., Any]] = {},
    extra_edges: Sequence[tuple[str, str]] = (),
    entry: str = "guard_input",
    done_node: str = END,
) -> CompiledStateGraph:
    """按 5 节点骨架装配图；`acall_model` 用 `partial(_wrap_model, ...)` 取得模型链。"""
```

`_wrap_model` 必须**模块级可导出**（`code_reviewer.wrap_model = partial(_wrap_model, ...)` 靠它），
而 `build_tool_agent_graph` 只是装配器。`done_node` 是条件边 `"done"` 的落点：默认 `END`，
`code_reviewer` 传 `"remember_review"`（§7.3）。

这**同时就是 §6.2 第 1 步的落点** —— 折叠只有这一处读取点。

共享 state（`total=False`，对没有相应节点的 agent 无害）：

```python
class AgentState(MessagesState, total=False):
    safety: SafeguardOutput
    remaining_steps: RemainingSteps
    recalled_reviews: str
    distilled_summary: str
```

### 7.3 `code_reviewer` 侧只留钩子

```python
def _recall_suffix(state: AgentState) -> str:
    recalled = state.get("recalled_reviews") or ""
    if not recalled:
        return ""
    return (
        "\n\n以下是**以往对同一用户的审查结论**（来自长期记忆，按相关度排序）。"
        "若与本次问题相关，请参考它们保持一致，并明确指出哪些是历史结论、"
        "哪些是本次新发现：\n\n" + recalled
    )


wrap_model = partial(_wrap_model, tools=tools, instructions=instructions,
                     system_suffix=_recall_suffix)

code_reviewer = build_tool_agent_graph(
    tools=tools,
    instructions=instructions,
    system_suffix=_recall_suffix,
    extra_nodes={
        "recall_reviews": recall_reviews,
        "remember_review": remember_review,
        "distill_history": distill_history,
    },
    extra_edges=[("recall_reviews", "distill_history"), ("distill_history", "model")],
    done_node="remember_review",
)
code_reviewer.name = "code-reviewer"
```

实现上有四点不能漏：

1. `wrap_model` 必须继续从 `agents.code_reviewer` 导出（§1 接缝）。
2. 工厂**不要反向 import `agents.agents`**（`AgentGraph` 定义在那里，会成环）。返回类型标 `CompiledStateGraph`。
3. 每个 agent 保留模块级 `tools` / `instructions`（测试与可读性都依赖）。
4. 节点名与边**一个都不许改**（§1 接缝）。

**`pyproject.toml` 的 `bad-specialization`（`:129`）不必改** —— 注释（`:126-128`）说的是
「每个 agent 的 `StateGraph(AgentState)` 都会触发」，未给计数；改动只是让实际触发点从 8 降到 6
（`command_agent` / `bg_task_agent` / `loop_agent` / `interrupt_agent` / `knowledge_base_agent` + 工厂 1 处），
措辞仍然成立。

### 7.4 范围决定

- **第 1 批（推荐）**：`research_assistant` / `rag_assistant` / `code_reviewer` 并入。
  三者共享同一套 `guard_input` 安检链，语义完全一致。
- **`loop_agent`（可选第 2 批）**：并入需把步数阈值（`<= 1` vs `< 2`）与超限文案变成工厂参数，
  它没有安检节点，用 `entry="model"`，`done_node` **取默认值 `END` 即可**（实测 `loop_agent.py:76`
  的条件边正是 `{"tools": "tools", "done": END}`，与工厂默认一致，不必传 `None`——v3 写的
  `done_node=None` 会与默认值语义打架）。收益约 30 行，
  **代价是给它多挂 `safety` 通道**（或让工厂对不需要安检的 agent 不声明该通道）。单独决策。
- **`knowledge_base_agent` / `bg_task_agent` 不并入**：`wrap_model` 语义差异大
  （前者注入检索文档，后者直通），硬并会为了复用而牺牲可读性。

---

## 8. 第 1 项：client 同步/异步并轨

> **风险最高的一项**，因为它改变同步方法的调用路径。建议放在最后做。

### 8.1 三个辅助函数

```python
T = TypeVar("T")


def _run_sync(coro: Coroutine[Any, Any, T]) -> T:
    """在同步方法里跑一个一次性协程。

    streamlit_app.py:178（位于 async def main() 内，脚本由 :718 的
    asyncio.run 驱动）会调用本类的同步方法。streamlit 1.59.2 的
    @st.cache_data 在当前线程直接执行被缓存函数（cache_utils.py:385），
    没有线程池卸载，因此此时裸用 asyncio.run 会抛
    「cannot be called from a running event loop」。检测到运行中的 loop
    就退到工作线程。
    """
    try:
        asyncio.get_running_loop()
    except RuntimeError:
        return asyncio.run(coro)
    with ThreadPoolExecutor(max_workers=1) as pool:
        return pool.submit(asyncio.run, coro).result()


def _iterate_sync(agen: AsyncGenerator[T, None]) -> Generator[T, None, None]:
    """同步迭代异步生成器。

    必须在同一个 loop 上逐个 __anext__：每次新建 loop 会让已绑定的
    httpx 连接池落到已关闭的 loop 上。finally 里 aclose 是为了调用方
    提前 break 时也能关闭响应流。
    """
    loop = asyncio.new_event_loop()
    try:
        while True:
            try:
                yield loop.run_until_complete(agen.__anext__())
            except StopAsyncIteration:
                return
    finally:
        loop.run_until_complete(agen.aclose())
        loop.close()
```

### 8.2 三个同步方法变薄委托

只并轨**有异步孪生**的三个。`retrieve_info`（`:62`）、`get_history`（`:345`）、
`acreate_feedback`（`:321`）**没有**同步/异步对，保持原样 —— 顺手给它们也造异步版属于超范围。

```python
    def invoke(self, message, model=None, thread_id=None, user_id=None, agent_config=None):
        """同步调用 agent。仅返回最终消息。"""
        return _run_sync(self.ainvoke(message, model=model, thread_id=thread_id,
                                      user_id=user_id, agent_config=agent_config))

    def stream(self, message, model=None, thread_id=None, user_id=None,
               agent_config=None, stream_tokens=True):
        """同步流式输出 agent 的响应。"""
        return _iterate_sync(self.astream(message, model=model, thread_id=thread_id,
                                          user_id=user_id, agent_config=agent_config,
                                          stream_tokens=stream_tokens))

    def get_user_threads(self, user_id, agent=None, limit=20):
        """列出用户的对话 thread。"""
        return _run_sync(self.aget_user_threads(user_id, agent=agent, limit=limit))
```

各自 docstring 里的 `Args:` 段随实现一起挪进异步孪生，避免两处重复维护。

**一处刻意的行为统一：** `astream:315-317` 有空 token 守卫（`if parsed != ""`），
同步 `stream` 没有。并轨后同步侧继承该语义 —— **这是有意的行为变更**，写进 commit message。

### 8.3 测试影响（v2 漏掉的关键一节）

现有 `tests/client/test_client.py` 的同步测试打的是**模块级** `httpx` 缝，
而异步实现走 `httpx.AsyncClient`。并轨后**打桩失效 → 变成对 `http://test` 的真实请求 →
抛 `AgentClientError` → 测试红**（会响亮失败，不是静默通过）。

| 测试 | 打桩行 | 现缝 | 处置 |
| --- | --- | --- | --- |
| `test_invoke` | `:57,64,79` | `httpx.post` | **重写** → 委托断言 + 嵌套守卫 |
| `test_stream` | `:147,169` | `httpx.stream` | **重写** → 同上 |
| `test_get_user_threads` | `:374,389,400,410` | `httpx.get` | **重写** → 同上 |
| `test_get_history` | `:282,292,300` | `httpx.post` | 保留（`get_history` 未并轨） |
| `test_info` | `:322,334` | `httpx.get` | 保留（`retrieve_info` 未并轨） |
| `test_ainvoke` / `test_astream` / `test_aget_user_threads` | — | `AsyncClient.*` | 保留，不改 |
| `tests/app/test_streamlit_app.py` | — | 整体 mock `AgentClient` | 天然盲区，**不指望它拦住回归** |

**不推荐**把 `test_stream` 改打成 `httpx.AsyncClient.stream` —— `test_astream:196-208`
为此已经写了完整的 `AsyncMock` + `__aenter__` + `aiter_lines` 装备，照抄一份等于把 HTTP 语义测两遍。
改为「委托 + 守卫」两类断言：

```python
def test_sync_invoke_delegates_to_async(agent_client) -> None:
    """并轨后同步方法只是薄委托：断言转发，不重复测 HTTP。"""
    with patch.object(agent_client, "ainvoke", new=AsyncMock(return_value="R")) as m:
        assert agent_client.invoke("q", model="m", thread_id="t", user_id="u") == "R"
    m.assert_awaited_once_with("q", model="m", thread_id="t", user_id="u", agent_config=None)


def test_sync_stream_delegates_to_async(agent_client) -> None:
    async def fake_astream(*args, **kwargs):
        yield "tok"

    with patch.object(agent_client, "astream", new=fake_astream):
        assert list(agent_client.stream("q")) == ["tok"]


@pytest.mark.asyncio
async def test_sync_call_inside_running_loop(agent_client) -> None:
    """守卫 streamlit_app.py:178 的嵌套场景。

    反例：把 _run_sync 的 ThreadPoolExecutor 分支去掉，本用例必须变红
    （asyncio.run 在运行中的 loop 内必抛 RuntimeError）。
    """
    with patch.object(agent_client, "aget_user_threads", new=AsyncMock(return_value="T")):
        assert agent_client.get_user_threads("u") == "T"
```

`test_sync_call_inside_running_loop` 是本项**唯一不可替代**的用例：其余行为都被异步孪生的
测试覆盖，只有「在运行中的 loop 里调同步方法」是并轨新引入的路径。

---

## 9. 第 6 项：schema 层 UI 方法归位

### 9.1 搬迁

- `ChatMessage.pretty_repr` / `pretty_print`（`schema/schema.py:126-137`，12 行）
  → `run_client.py` 的模块级 `_print_message()`。调用点 `run_client.py:26,34,50,58`。
  **注意** `run_agent.py:27` 也写 `.pretty_print()`，但那是 **LangChain 的 `AIMessage`**
  （自带 `pretty_print`），不受影响，**别顺手改它**。
- `TaskDataStatus`（`schema/task_data.py:37-74`，38 行）→ `streamlit_app.py` 内部类。
  `streamlit_app.py:13` 收缩为 `from schema.task_data import TaskData`；`:581` 的
  `TaskDataStatus()` 调用不变。`pyproject.toml:122` 已把 `streamlit_app.py` 排除在 pyrefly 外，
  搬迁后无类型负担。

### 9.2 连带项（v2 漏掉）

`README.md:190-201` 的客户端示例**公开**演示了 `response.pretty_print()`：
`run_client.py` 的 `ChatMessage` 一旦没有该方法，README 的示例立刻是错的。
搬迁时必须同时改 `README.md:195`（例如改为 `print(response.content)`，并删掉 `:196-199` 的
「Ai Message」框线注释）。**README 是 docs 的唯一入口，漂移了比代码注释更显眼。**

### 9.3 收益口径

**这是归位不是删减，净 LOC ≈ 0。** 价值在分层（schema 只放数据契约，展示与 UI 归调用方）。
不要记进重构收益，也不要为了凑行数顺手删 `TaskData`。

**验收** `uv run pytest tests/app tests/schema -q`。

---

## 10. 执行顺序

| 批次 | 内容 | 风险 | 主要验证 |
| --- | --- | --- | --- |
| 0 | 第 5 项（3 行）+ 第 4 项（删 8 行） | 极低 | `pytest tests/service/test_service_e2e.py tests/agents`、`pyrefly check` |
| 1 | 第 8 项（先删死分支，再换循环） | 低 | `pytest tests/agents/test_interrupt_agent.py tests/service/test_service_real_graphs.py` |
| 2 | 第 3 项（voice 基类） | 低 | `pytest tests/voice` |
| 3 | **第 7 项**（§6.2 五项 + §6.6 文档） | **中**——唯一正确性改动 | `probe_p07_integration.py` 退出码 0；`probe_p07_invariants.py` 全绿 |
| 4 | 第 2 项（工厂，含把 §6.2 第 1 步搬进去） | 中 | `pytest tests/agents tests/service`、`pyrefly check` |
| 5 | 第 1 项（先加回归测试，再并轨） | **最高** | `pytest tests/client tests/app` |
| 6 | 第 6 项（归位 + README） | 低 | `pytest tests/app tests/schema`；`pymarkdown scan README.md docs/ -r` |

**顺序理由。** 第 7 项与第 2 项互相成就 —— 第 2 项的共享视图构造**就是**第 7 项要的唯一读取点。
先做第 7 项则折叠先落在 `code_reviewer.wrap_model`，到第 2 项时再上提到工厂；两序皆可，
但**第 7 项的探针验收要在第 2 项之后再跑一次**。

第 1 项放最后：它是唯一会**改变既有同步调用路径**的改动，且第 8.3 节要重写 3 个测试。
在它的前面留一个干净的绿色基线，出问题时才能一口断定是它。

---

## 11. 需要你确认的决策点

1. **第 7 项的方向**（`docs/15` §7.2.3 已定，本文沿用）：**删 `_distill_input`、折叠进 model 节点**。
   若你的原意是「保留 service 侧作为唯一读取点」，那需要先解决 §0.2 第 3 条那个已实测的硬约束
   （`add_messages` 无法折叠既有 state）—— 请先确认再动手。
2. **`loop_agent` 是否并入工厂**（§7.4）。收益约 30 行，代价是给它多挂 `safety` 通道。
3. **第 1 项的同步 `stream` 行为统一**（§8.2 末）：并轨后同步流不再产出空 token。
   这是有意的语义变更，也可以选择在 `_iterate_sync` 里补一个等价过滤来保持完全兼容。
4. **`README.md:59` 与 `:108` 的文档编号区间**：本身 docs 已是 17 篇（磁盘含 `docs/16` 与已存在的 `docs/17-审查台账.md`，均未跟踪）。
   `:59` 的 `01`–`15` 与 `:64` 的 `10`–`15` 需连同 `### Documentation` 表补 16、17 两行一起改；
   `:108` 的 `10`–`20` / `01`–`20` 仍成立。**注意** `docs/17` 不是本次新增——它已作为独立台账在盘上，
   提交时应确认与 `docs/16` 的职责边界（台账=证据记录，方案=落地改法），不要合并。

---

## 12. 本次不做

- **正确性类，另开立项**：`determine_birthdate` 就地 `append` 到 state（§4.2 刻意保留）、
  `loop_agent` 的步数阈值语义（`< 2` vs `<= 1`）。
  `_distill_input` 造成的 checkpoint 膨胀由第 7 项顺手消掉，不单列。
- **删语音模块**：`docs/13` 记录语音按设计关闭；真要砍，收益大于合并 STT/TTS，属产品决策。
- **`knowledge_base_agent` / `bg_task_agent` 并入工厂**：见 §7.4。
- **`custom_data` 加 `extra="forbid"`**：透传通道收窄是正确性改动，见 §3。
- **两棵树的分歧治理**：建议主线提交后，让
  `C:\Users\Lenovo\WorkBuddy\Worktrees\agent-tool\main-dd61080f` 跟随主线或直接弃用，
  避免下一次审计再撞同一个坑。
