# 2026-09-18 审查台账（8 项核实与修复跟踪）

## 定位声明

本台账是**登记与索引**，不是判定真相的持有者。三条边界：

- **状态真相**在 `docs/15` §7.2（P0 项状态）与 `docs/16`（方案、接缝、验收）。
- **事实真相**在被引用的源码与测试本身；台账只登记「文件:行 + 文件指纹」。
- 台账与上述任一处冲突时，**以它们为准**，并回头修正本台账。

这样定性的原因：本仓已有 `docs/10-审核与修复总账.md`（2026-09-17 轮）。
若台账也宣布自己是真相来源，同一项状态立刻出现三处副本，必然漂移 ——
而 `docs/15` §7.2 已明确宣称自己是 P0 状态的唯一真相。台账的价值在**可勾选、可追溯、可交接**，
不在另立一套结论。

---

## 一、审查基线

| 项 | 值 |
| --- | --- |
| 仓库 | `D:\学习笔记\Agent\agent-tool` |
| HEAD | `f257c91`（`feat: 记忆与检索层 P0-1…P0-7 落地`） |
| 工作树 | 干净；未跟踪 = 本文档、`docs/16-精简与重构方案.md`、2 个与本仓无关的产物 |
| 复核日期 | 2026-09-18 |
| 审查对象 | 8 项由外部审计提出、经**独立复核**的结论 |
| 复核原则 | **不采信上游结论**，逐项自行取证；结论对不等于落地不会炸 |
| 另一棵工作树 | `C:\Users\Lenovo\WorkBuddy\Worktrees\agent-tool\main-dd61080f`（同一 HEAD，工作树干净）—— **行号对不上时先查树，再查代码** |

### 1.1 文件指纹（md5 前 8 位）

行号漂移时的第一判据：**指纹一致 = 结论错了；指纹不一致 = 代码变了**。

| 文件 | 指纹 | 本轮涉及 |
| --- | --- | --- |
| `src/client/client.py` | `e3320e93` | #1 |
| `src/agents/code_reviewer.py` | `c1ed88f5` | #2 #7 |
| `src/voice/stt.py` | `ba1f03d4` | #3 |
| `src/voice/tts.py` | `142d14d9` | #3 |
| `src/run_agent.py` | `e290a254` | #4 |
| `src/agents/utils.py` | `a26fae35` | #5 |
| `src/schema/schema.py` | `e3679af7` | #6 |
| `src/schema/task_data.py` | `9a2fc7a4` | #6 |
| `src/agents/interrupt_agent.py` | `30eead79` | #8 |
| `src/streamlit_app.py` | `103dc10b` | #1（调用方）#6（落点） |

---

## 二、复核手段

| 手段 | 用于 | 判据要点 |
| --- | --- | --- |
| `wc -l` | #3 | 行数类陈述直接量 |
| `md5sum` | 基线 | 区分「代码变了」与「结论错了」 |
| `ast` 抽源码 + 精确 / 去注释两档分组 | #2 | 脚本 `var/verify_dup_functions.py`；两档都报，避免把「注释不同」误报成「逻辑不同」 |
| pydantic 实例化实测 | #5 | 断言的是**静默丢弃**（不报错），不是「会报错」 |
| 读安装版库源码 | #1 #7 #8 | `langgraph.store.base.aget` 返回注解；`streamlit` 缓存是否线程卸载 |
| 全仓调用点穷举 | #7 | `apply_distillation` 的唯一调用点即判决性证据 |
| 穷举依赖它的测试 | 全部 | patch 缝是否同层、`match=` 断言是否逐字 —— **重构方案最容易在这里炸** |

---

## 三、台账主表

状态口径见 §5。**方案列**指向 `docs/16-精简与重构方案.md` 的章节。

| # | 项 | 复核结论 | 影响面 | 状态 | 方案 | 批次 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `client.py` 三对同步/异步重复 | ✅ 真 | 可维护性 | 待修 | §8 | 5 |
| 2 | 7 个框架函数重复 | ✅ 真（措辞需修正，见 §4.2） | 可维护性 | 待修 | §7 | 4 |
| 3 | `stt.py` 157 / `tts.py` 173 行，同骨架双写 | ✅ 真 | 可维护性 | 待修 | §5 | 2 |
| 4 | `run_agent.py:29-36` 过时注释块 | ✅ 真 | 注释与文档 | 待修 | §2 | 0 |
| 5 | `CustomData` 无 `type`，调用点传 `type=` 被静默丢弃 | ✅ 真 | 声实不符 | 待修 | §3 | 0 |
| 6 | `pretty_repr` / `pretty_print` / `TaskDataStatus` 分层不当 | ✅ 真 | 可维护性 | 待修 | §9 | 6 |
| 7 | 蒸馏读取侧两条路径都断 | ✅ 真 | **正确性** | **部分落地**（写侧已提交，读取侧待修） | §6 | 3 |
| 8 | `isinstance(result, list)` 死分支 + 误导注释 | ✅ 真 | 声实不符 | 待修 | §4 | 1 |

**唯一正确性缺陷是 #7。** 其余三项「可维护性」、两项「声实不符」、一项「注释与文档」。
除 #7 外**都不应与 #7 混进同一个提交**。

---

## 四、逐项证据链

### 4.1 第 1 项：client 三对同步/异步重复

| 内容 | 值 |
| --- | --- |
| 对 1 | `ainvoke:88` / `invoke:134` |
| 对 2 | `stream:204` / `astream:261` |
| 对 3 | `get_user_threads:376` / `aget_user_threads:401` |
| 已抽过的部分 | `_parse_stream_line:179`、`_user_threads_request:369` |
| 差异 | `astream:315-317` 有空 token 守卫，`stream` 没有（**行为分叉**，见 #1 验收） |
| 无异步孪生（**不得并轨**） | `retrieve_info:62`、`get_history:345`、`acreate_feedback:321` |
| 复核新增风险 | 3 个同步测试打的是**模块级** `httpx` 缝（`test_client.py:57,64,79` / `:147,169` / `:374,389,400,410`），与 `httpx.AsyncClient` **不同层** → 并轨后打桩失效、变成真实请求 |

### 4.2 第 2 项：7 个框架函数重复

函数名与覆盖面（**比审计原措辞更大**）：

| 函数 | 出现处数 | 逐字程度 |
| --- | --- | --- |
| `wrap_model` | 6 | **已分化**（`code_reviewer` 那版多注入 `recalled_reviews`）—— 不算重复 |
| `acall_model` | 6 | 仅差一句中文注释 |
| `pending_tool_calls` | 4 | 含注释逐字（含 `loop_agent`） |
| `format_safety_message` | 3 | 含注释逐字 |
| `safeguard_input` | 3 | 含注释逐字 |
| `block_unsafe_content` | 3 | 含注释逐字 |
| `check_safety` | 3 | 含注释逐字 |

**结论对，但「逐字重复」只有 5 个严格成立。** 复核脚本：`var/verify_dup_functions.py`。

| 必须保住的接缝 | 位置 |
| --- | --- |
| `agents.code_reviewer.wrap_model` 被测试直接 import | `tests/agents/test_memory_read_path.py:203,213` |
| 节点名与边 | `tests/agents/test_code_reviewer.py:25`、`test_memory_read_path.py:249-252` |

### 4.3 第 3 项：STT / TTS 同骨架双写

| 内容 | 值 |
| --- | --- |
| 行数 | `stt.py` 157、`tts.py` 173（`wc -l` 实测） |
| 真正的重复代码 | 约 25 行/文件（其余是 docstring） |
| 共享骨架 | `__init__` → `_get_api_key` → `_load_provider` → `provider` → `from_env` |
| 模块路径**不得变** | `voice/__init__.py:34-35`、`voice/manager.py:12-13`、`tests/voice/test_manager.py:31-32` |
| 错误串**不得变** | `tests/voice/test_stt.py:22,29`、`tests/voice/test_tts.py:22,29` 四处 `match=` |

### 4.4 第 4 项：`run_agent.py` 注释块

| 内容 | 值 |
| --- | --- |
| 待删 | `:29-36`，8 行 pygraphviz 指引 |
| **必须保留** | `:9` `CompiledStateGraph` import、`:16` `cast(...)`（`get_agent()` 返回 `CompiledStateGraph \| Pregel`，`cast` 是收窄） |
| **整文件不要删** | `docs/10:169` 与 `code_reviewer.py` 注释都引用它作为「脱离服务直接调图」的场景 |

### 4.5 第 5 项：`CustomData.type` 静默丢弃

| 内容 | 值 |
| --- | --- |
| 类定义 | `agents/utils.py:8-11`，字段只有 `data` |
| 丢参点 | `bg_task_agent/task.py:25` 传 `type=self.name` |
| 实测 | pydantic **2.13.4**：构造**不报错**，`model_dump()` 只剩 `{'data': ...}`，`model_config == {}`（extra 默认 ignore） |
| 传参路径 | `to_langchain()` 只把 `self.data` 塞进 `content`，`type` 即使补上字段也不会外发 |
| 消费方 | `streamlit_app.py:566` 用 `TaskData.model_validate(msg.custom_data)`，靠 `TaskData` 字段辨认，**无人读 `type`** |
| 线上形状契约 | `tests/service/test_service_e2e.py:49,68`、`test_service_real_graphs.py:236` |
| 处置 | **删 3 处实参**，不补字段（补字段会改线上形状） |

### 4.6 第 6 项：schema 层 UI 方法

| 内容 | 值 |
| --- | --- |
| 待搬 | `schema/schema.py:126-137`（12 行，`pretty_repr`/`pretty_print`）→ `run_client.py` |
| 调用点 | `run_client.py:26,34,50,58` |
| 待搬 | `schema/task_data.py:37-74`（38 行 `TaskDataStatus`）→ `streamlit_app.py` |
| 调用点 | `streamlit_app.py:13,581` |
| **不受影响** | `run_agent.py:27` 的 `.pretty_print()` 是 **LangChain `AIMessage` 自带**的，别顺手改 |
| **连带项** | `README.md:195` 公开示例就是 `response.pretty_print()` → 搬迁后示例立即失效（必须同批改） |
| 收益口径 | **归位不是删减，净 LOC ≈ 0**，不计入重构收益 |

### 4.7 第 7 项：蒸馏读取侧两条路径都断（唯一正确性缺陷）

| 内容 | 值 |
| --- | --- |
| 写侧（**已落地**） | 节点注册 `code_reviewer.py:192`；实现 `core/distill.py:245`；边 `:214` `:217` |
| 图内读取侧（**断**） | `acall_model` `code_reviewer.py:73-88` 直接用 `state["messages"]`，从不调 `apply_distillation` |
| 服务读取侧（**断且为负**） | `_distill_input` `service.py:234-267`，调用点 `:217-224` |
| **判决性证据** | `apply_distillation` 全仓调用点**唯一** = `service.py:256`；`agents/` 目录**零调用** |
| **严重性证据** | `apply_distillation` 返回 `[新 HumanMessage, *tail]`，`tail` 是**同 id 的原消息副本**、摘要是**无 id 的新消息** → 过 `add_messages` 后同 id 原地替换、摘要被**追加到末尾**。历史折不掉，且**每轮多囤一份摘要副本**（`24→27→30→33→36`） |
| 后果 | checkpoint 无界增长；摘要副本污染 `messages` |
| 注释与实际相反 | `service.py:217-222` 写「改写的是送给模型的这一份」，与 reducer 语义不符 |
| 方向 | `docs/15` §7.2.3 已定：**折叠收敛到 model 节点的视图构造，删掉 `_distill_input`** |
| 专用用例（待删） | `tests/core/test_distill.py:290,304,323,331` |
| 验收探针 | `var/probe_p07_integration.py`（退出码 0）、`var/probe_p07_invariants.py`（5 条不变量） |
| 注入器注意 | `var/p07_inject.py` 必须带旁路备份 + 启动自愈（实测漏过一次，注入残留在 `src/core/distill.py`） |

### 4.8 第 8 项：`interrupt_agent` 死分支

| 内容 | 值 |
| --- | --- |
| 死分支 | `:98-107`，具体 `:103 if isinstance(result, list):` |
| 误导注释 | `:100`「处理 store.aget 可能直接返回 Item 或返回列表的情况」 |
| 判决依据 | `BaseStore.get`/`aget` 返回注解均为 `Item \| None`（`langgraph/store/base/__init__.py:762,1007`，batch mixin `base/batch.py:88` 同）；`src/memory/*` 无任何 `aget` 覆写 |
| 附带项（另开） | 自递归 `:160`、`:173` 可改循环；`:158`/`:171` 的 `state["messages"].append(...)` 就地语义**刻意保留**（正确性范畴，见 `docs/16` §4.2） |

---

## 五、状态定义与流转

| 状态 | 含义 | 判据 |
| --- | --- | --- |
| `待修` | 无任何相关代码改动 | 本表初始态 |
| `部分落地` | 同一项内部有的部分已提交 | 仅 #7（写侧已落地） |
| `修复中` | 分支上已动手，未过验收 | 有改动、验收未全绿 |
| `已修待验` | 改动完成，等跑验收命令 | 验收命令未执行 |
| `已验收` | 验收命令全绿 | 附命令与实际输出 |
| `不做（已决策）` | 有意不做 | 须在 `docs/16` §12 有对应条目 |

**记账纪律：** 状态只在验收命令**实际跑绿**后才可置 `已验收`；
每次状态变更须同时更新本表与 `docs/15` §7.2（P0 项）。

---

## 六、验收与复现

### 6.1 逐批验收命令

| 批次 | 验收 |
| --- | --- |
| 0（#4 #5） | `uv run pytest tests/service/test_service_e2e.py tests/agents -q` |
| 1（#8） | `uv run pytest tests/agents/test_interrupt_agent.py tests/service/test_service_real_graphs.py -q` |
| 2（#3） | `uv run pytest tests/voice -q` |
| 3（#7） | `uv run python var/probe_p07_integration.py` + `var/probe_p07_invariants.py` + `pytest tests/core/test_distill.py tests/agents/test_memory_read_path.py -q` |
| 4（#2） | `uv run pytest tests/agents tests/service -q` + `uv run pyrefly check` |
| 5（#1） | `uv run pytest tests/client tests/app -q` |
| 6（#6） | `uv run pytest tests/app tests/schema -q` + `pymarkdown scan README.md docs/ -r` |

### 6.2 每批收尾

```bash
uv run pytest -q
uv run ruff check . && uv run ruff format --check .
uv run pyrefly check
```

> 裸跑 `pytest -q` 若「测试全绿但没有摘要行、退出码 1」，是收尾批量删除
> `%Temp%\pytest-of-Lenovo\garbage-*`（>50 文件）撞沙箱闸门，**不是测试失败**。

### 6.3 本轮的复核脚本

| 脚本 | 作用 | 退出码 |
| --- | --- | --- |
| `var/verify_dup_functions.py` | #2：`ast` 抽 7 个函数的源码，按精确/去注释两档分组 | 0 = 存在重复 |
| `var/probe_p07_integration.py` | #7：模型收到的条数是否随轮次增长 | 0 = 已修；1 = 缺陷仍在 |

---

## 七、复核修正记录

台账的意义之一是不让人重复争议同一件事。本轮修正如下。

### 7.1 判定沿革

| 版本 | 第 7 项判定 | 原因 |
| --- | --- | --- |
| v1 | ❌ 不成立 | **误用另一棵 worktree 作基线**（那棵树停在 HEAD，确实没有 `core/distill.py`） |
| v2 | ✅ 存在 + 残缺陷成立 | 修正基线后复核 |
| v3（本轮） | ✅ 真，且**严重性上调** | 补上「净增量」证据（§4.7） |

**教训已固化：两棵树的 `git status` 不一致时，先查树，再查代码。**

### 7.2 措辞修正（结论方向不变）

| # | 上游措辞 | 修正 |
| --- | --- | --- |
| 2 | 「7 个函数**逐字**重复」 | 只有 5 个严格逐字；`acall_model` 仅注释差、`wrap_model` 已分化；覆盖面是 6/6/4 处（不是 3/3/3） |
| 6 | 「存在」 | 属实，但性质是**归位**、净 LOC ≈ 0；且有 `README.md:195` 连带项 |
| 7 | 「断点集中于…」 | 两半都对，但 service 路径不是「无效」而是**净增量** |

### 7.3 行号漂移（已校准）

| 位置 | 旧述 | 实际 |
| --- | --- | --- |
| `pyproject.toml` `bad-specialization` | `:126` | **`:129`**（且注释未给计数，**不必改**） |
| `docs/15` §7.2.3 引用 `code_reviewer.py:59` | `:59` | **`:73`** |
| streamlit 版本 | `1.56` | **`1.59.2`**（结论方向不变） |

---

## 八、未纳入本台账的事项

以下为**已知但不在本轮审查范围**，单列以免被误认为「已覆盖」：

| 事项 | 归属 |
| --- | --- |
| P0-6 golden set / P0-8 混合检索 / P0-9 索引对账 | `docs/15` §7.2 |
| loop-agent 三项改动（共享止损常量 / `fetch_url` / 搜索预算） | `docs/11` §1 |
| `determine_birthdate` 就地 `append` 到 state | `docs/16` §12（正确性范畴，另开） |
| `loop_agent` 步数阈值语义（`< 2` vs `<= 1`） | `docs/16` §12 |
| 天气工具换 `wttr.in`、safeguard 复用 DeepSeek key | 已决策、未动手 |
| 两棵树的分歧治理 | `docs/16` §12（建议弃用或 `git reset --hard` 跟随主线） |
| `knowledge_base_agent` / `bg_task_agent` 并入工厂 | `docs/16` §7.4（有意不并） |

---

## 九、文档集成待办

本台账与 `docs/16` 目前均**未跟踪**。若决定随代码入版本，须**同批**完成：

| 位置 | 现状 | 改后 |
| --- | --- | --- |
| `README.md:59` | flat and numbered `01`–`15` | flat and numbered `01`–`17` |
| `README.md:61-63` | …and `10`–`15` are engineering records | …and `10`–`17` are engineering records |
| `README.md` 的 `### Documentation` 表 | 表项止于 `15` | 补 `16`、`17` 两行 |
| `README.md:108` | `10`–`20` 与 `01`–`20` | **不变**（区间仍覆盖） |

改完按文档规范收尾：

```bash
uv run python var/research/check_docs.py .
uv run python var/research/check_md_tables.py <改动文件>
pymarkdown scan README.md docs/ -r
```
